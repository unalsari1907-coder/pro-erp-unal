name: Apply PRO ERP 2.5.31 finance and shortcuts v3

on:
  push:
    paths:
      - '.github/workflows/apply_finance_shortcuts_2531_v3.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply robust 2.5.31 patch
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          def rep(text, old, new, label):
              if new in text:
                  return text
              if old not in text:
                  raise SystemExit(f'Bulunamadi: {label}')
              return text.replace(old, new, 1)

          # ==================== KASA / BANKA ====================
          p = Path('lib/screens/kasa_banka_sayfasi.dart')
          s = p.read_text(encoding='utf-8')

          s = rep(s,
          '''  final TextEditingController _aramaController = TextEditingController();

  bool _yukleniyor = true;''',
          '''  final TextEditingController _aramaController = TextEditingController();
  final FocusNode _aramaFocusNode = FocusNode(debugLabel: 'kasaArama');

  bool _yukleniyor = true;''', 'kasa focus node')

          s = rep(s,
          '''    _aramaController.dispose();

    super.dispose();''',
          '''    _aramaController.dispose();
    _aramaFocusNode.dispose();

    super.dispose();''', 'kasa dispose')

          s = s.replace(".select('kasa_id, kasa_adi, kasa_tipi')", ".select('kasa_id, kasa_adi, kasa_tipi, kk_limit')")

          marker = '''  double _kasaBakiyesi(int kasaId) {'''
          helper = '''  bool _krediKartiMi(Map<String, dynamic> kasa) {
    final ad = _metin(kasa['kasa_adi']).toUpperCase();
    final tip = _metin(kasa['kasa_tipi']).toUpperCase();
    return tip.contains('POS') ||
        tip.contains('KART') ||
        ad.contains('POS') ||
        ad.contains('K.K') ||
        ad.contains('KREDI') ||
        ad.contains('KREDİ') ||
        ad.contains('KART');
  }

  double _kkLimit(Map<String, dynamic> kasa) => _sayi(kasa['kk_limit']);

  double _kkKullanilan(Map<String, dynamic> kasa) {
    final id = int.tryParse(kasa['kasa_id']?.toString() ?? '') ?? 0;
    final bakiye = _kasaBakiyesi(id);
    return bakiye < 0 ? -bakiye : 0.0;
  }

  double _kkKalan(Map<String, dynamic> kasa) {
    final kalan = _kkLimit(kasa) - _kkKullanilan(kasa);
    return kalan < 0 ? 0 : kalan;
  }

  Future<void> _kkLimitDuzenle(Map<String, dynamic> kasa) async {
    final kasaId = int.tryParse(kasa['kasa_id']?.toString() ?? '') ?? 0;
    if (kasaId <= 0) return;

    final controller = TextEditingController(
      text: _kkLimit(kasa) == 0 ? '' : _kkLimit(kasa).toStringAsFixed(2),
    );

    final kaydet = await showDialog<bool>(
      context: context,
      builder: (ctx) => AlertDialog(
        title: Text('${_metin(kasa['kasa_adi'])} • K.K Limiti'),
        content: TextField(
          controller: controller,
          autofocus: true,
          keyboardType: const TextInputType.numberWithOptions(decimal: true),
          inputFormatters: [
            FilteringTextInputFormatter.allow(RegExp(r'[0-9,.]')),
          ],
          decoration: const InputDecoration(
            labelText: 'Kredi Kartı Limiti',
            prefixIcon: Icon(Icons.credit_card_rounded),
            suffixText: '₺',
            border: OutlineInputBorder(),
          ),
        ),
        actions: [
          TextButton(
            onPressed: () => Navigator.pop(ctx, false),
            child: const Text('Vazgeç'),
          ),
          FilledButton.icon(
            onPressed: () => Navigator.pop(ctx, true),
            icon: const Icon(Icons.save_rounded),
            label: const Text('Kaydet'),
          ),
        ],
      ),
    );

    if (kaydet != true) {
      controller.dispose();
      return;
    }

    final limit = double.tryParse(controller.text.trim().replaceAll(',', '.')) ?? 0.0;
    controller.dispose();

    if (limit < 0) {
      _mesaj('Kredi kartı limiti negatif olamaz.', Colors.red);
      return;
    }

    try {
      await SupabaseService.supabase
          .from('kasalar')
          .update({'kk_limit': limit})
          .eq('kasa_id', kasaId);
      if (!mounted) return;
      setState(() => kasa['kk_limit'] = limit);
      _mesaj('Kredi kartı limiti güncellendi.', Colors.green);
    } catch (e) {
      if (mounted) {
        _mesaj('K.K limiti kaydedilemedi: $e', Colors.red);
      }
    }
  }

'''
          if helper not in s:
              if marker not in s:
                  raise SystemExit('Bulunamadi: kasa helper marker')
              s = s.replace(marker, helper + marker, 1)

          s = rep(s,
          '''      if (!mounted) return;

      _mesaj(
        _islemTipi == 'TAHSILAT' ''',
          '''      if (!mounted) return;

      final yeniHareket = <String, dynamic>{
        'hareket_id': 'yeni',
        'tarih': _islemTarihi.toIso8601String(),
        'kasa_id': kasaId,
        'tip': _islemTipi == 'TAHSILAT' ? 'GIRIS' : 'CIKIS',
        'tutar': tutar,
        'aciklama': aciklama,
        'cari_id': cariId,
        'kullanici': YetkiService.aktifKullanici,
        'belge_no': belgeNo,
        'cari_unvan': _metin(cari['unvan']),
        'kasa_adi': _metin(kasa['kasa_adi']),
      };
      setState(() {
        _tumHareketler = <Map<String, dynamic>>[
          yeniHareket,
          ..._tumHareketler,
        ];
      });
      _hareketleriFiltrele();

      _mesaj(
        _islemTipi == 'TAHSILAT' ''', 'instant kasa refresh')

          s = s.replace(
          '''      await _ilkVerileriYukle();
    } catch (e) {''',
          '''      await Future<void>.delayed(const Duration(milliseconds: 250));
      await _ilkVerileriYukle();
    } catch (e) {''', 1)

          s = rep(s,
          '''                          controller: _aramaController,
                          decoration: InputDecoration(''',
          '''                          controller: _aramaController,
                          focusNode: _aramaFocusNode,
                          decoration: InputDecoration(''', 'kasa search focus')

          s = rep(s,
          '''                          Expanded(
                            child: Text(
                              _metin(kasa['kasa_adi']),
                              maxLines: 2,
                              overflow: TextOverflow.ellipsis,
                              style: const TextStyle(
                                fontWeight: FontWeight.bold,
                              ),
                            ),
                          ),
                        ],
                      ),
                      const Spacer(),
                      Text(
                        _para(bakiye),''',
          '''                          Expanded(
                            child: Text(
                              _metin(kasa['kasa_adi']),
                              maxLines: 2,
                              overflow: TextOverflow.ellipsis,
                              style: const TextStyle(
                                fontWeight: FontWeight.bold,
                              ),
                            ),
                          ),
                          if (_krediKartiMi(kasa))
                            IconButton(
                              tooltip: 'K.K limitini düzenle',
                              visualDensity: VisualDensity.compact,
                              onPressed: () => _kkLimitDuzenle(kasa),
                              icon: const Icon(Icons.edit_rounded, size: 18),
                            ),
                        ],
                      ),
                      const Spacer(),
                      if (_krediKartiMi(kasa) && _kkLimit(kasa) > 0) ...[
                        Text(
                          'Limit: ${_para(_kkLimit(kasa))} • Kalan: ${_para(_kkKalan(kasa))}',
                          maxLines: 1,
                          overflow: TextOverflow.ellipsis,
                          style: TextStyle(
                            fontSize: 11,
                            fontWeight: FontWeight.w700,
                            color: _kkKalan(kasa) > 0
                                ? Colors.green.shade700
                                : Colors.red.shade700,
                          ),
                        ),
                        const SizedBox(height: 3),
                      ],
                      Text(
                        _para(bakiye),''', 'kasa kart limit')

          # Ctrl+F on Kasa page only. Esc is handled globally by Dashboard.
          s = rep(s,
          '''  @override
  Widget build(BuildContext context) {
    return Scaffold(''',
          '''  @override
  Widget build(BuildContext context) {
    return CallbackShortcuts(
      bindings: <ShortcutActivator, VoidCallback>{
        const SingleActivator(LogicalKeyboardKey.keyF, control: true): () {
          _aramaFocusNode.requestFocus();
        },
      },
      child: Focus(
        autofocus: true,
        child: Scaffold(''', 'kasa shortcuts open')

          s = rep(s,
          '''                Expanded(child: _hareketListesi()),
              ],
            ),
    );
  }
}''',
          '''                Expanded(child: _hareketListesi()),
              ],
            ),
        ),
      ),
    );
  }
}''', 'kasa shortcuts close')

          p.write_text(s, encoding='utf-8')

          # ==================== DASHBOARD / GLOBAL SHORTCUTS ====================
          p = Path('lib/screens/dashboard_sayfasi.dart')
          d = p.read_text(encoding='utf-8')

          if "import 'package:flutter/services.dart';" not in d:
              d = d.replace("import 'package:flutter/material.dart';", "import 'package:flutter/material.dart';\nimport 'package:flutter/services.dart';", 1)

          d = rep(d,
          '''  final TextEditingController _menuAramaController = TextEditingController();
  final ScrollController _menuScrollController = ScrollController();''',
          '''  final TextEditingController _menuAramaController = TextEditingController();
  final FocusNode _menuAramaFocusNode = FocusNode(debugLabel: 'erpMenuArama');
  final ScrollController _menuScrollController = ScrollController();''', 'dashboard focus node')

          d = rep(d,
          '''    _menuAramaController.dispose();
    _menuScrollController.dispose();''',
          '''    _menuAramaController.dispose();
    _menuAramaFocusNode.dispose();
    _menuScrollController.dispose();''', 'dashboard dispose')

          if 'focusNode: _menuAramaFocusNode' not in d:
              d = rep(d, 'controller: _menuAramaController,', 'controller: _menuAramaController,\n                focusNode: _menuAramaFocusNode,', 'dashboard search field')

          d = rep(d,
          '''  @override
  Widget build(BuildContext context) {
    return LayoutBuilder(''',
          '''  @override
  Widget build(BuildContext context) {
    return CallbackShortcuts(
      bindings: <ShortcutActivator, VoidCallback>{
        const SingleActivator(LogicalKeyboardKey.keyF, control: true): () {
          _menuAramaFocusNode.requestFocus();
        },
        const SingleActivator(LogicalKeyboardKey.escape): () {
          if (_seciliSayfa != 'dashboard') {
            setState(() => _seciliSayfa = 'dashboard');
          } else {
            Navigator.of(context).maybePop();
          }
        },
      },
      child: Focus(
        autofocus: true,
        child: LayoutBuilder(''', 'dashboard shortcuts open')

          d = rep(d,
          '''      },
    );
  }
}

class _AltMenuModel''',
          '''      },
        ),
      ),
    );
  }
}

class _AltMenuModel''', 'dashboard shortcuts close')

          p.write_text(d, encoding='utf-8')

          # ==================== SQL ====================
          sql = Path('supabase/PRO_ERP_FINANS_KK_LIMIT_2_5_31.sql')
          sql.write_text('''-- PRO ERP 2.5.31 - Kredi kartı limit takibi
alter table if exists public.kasalar
  add column if not exists kk_limit numeric(14,2) not null default 0;

comment on column public.kasalar.kk_limit is
  'POS/Kredi kartı için tanımlı toplam limit.';

notify pgrst, 'reload schema';
''', encoding='utf-8')
          PY

          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/screens/kasa_banka_sayfasi.dart lib/screens/dashboard_sayfasi.dart supabase/PRO_ERP_FINANS_KK_LIMIT_2_5_31.sql
          git diff --cached --quiet && exit 0
          git commit -m "Apply card limits instant cash refresh and keyboard shortcuts"
          git push origin HEAD:main
