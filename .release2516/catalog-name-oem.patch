--- a/lib/screens/arac_parca_katalog_sayfasi.dart	2026-08-15 13:41:58.483970602 +0000
+++ b/lib/screens/arac_parca_katalog_sayfasi.dart	2026-08-15 13:41:19.249990303 +0000
@@ -816,7 +816,7 @@
 
   Future<void> _parcaTamDuzenle(Map<String, dynamic> parca) async {
     final ust = TextEditingController(text: _parcaUstKategori(parca));
-    final ad = TextEditingController(text: _s(parca['kategori_adi']));
+    final ad = TextEditingController(text: _birlesikRlKategoriAdi(_s(parca['kategori_adi'])));
     final oem = TextEditingController(text: _s(parca['oem_kodu']));
     final kaydet = await showDialog<bool>(
       context: context,
@@ -1031,7 +1031,7 @@
     final kaydet = await showDialog<bool>(
       context: context,
       builder: (dialogContext) => AlertDialog(
-        title: Text('${_s(parca['kategori_adi'])} • OEM Güncelle'),
+        title: Text('${_birlesikRlKategoriAdi(_s(parca['kategori_adi']))} • OEM GUNCELLE'),
         content: SizedBox(
           width: 520,
           child: Column(
@@ -1119,7 +1119,7 @@
     final kaydet = await showDialog<bool>(
       context: context,
       builder: (dialogContext) => AlertDialog(
-        title: Text('${_s(parca['kategori_adi'])} • OEM Ekle'),
+        title: Text('${_birlesikRlKategoriAdi(_s(parca['kategori_adi']))} • OEM EKLE'),
         content: SizedBox(
           width: 480,
           child: TextField(
@@ -1929,11 +1929,31 @@
     return k;
   }
 
+  String _asciiBuyuk(String deger) {
+    var x = deger.trim().toUpperCase();
+    const tr = <String, String>{
+      'Ç': 'C',
+      'Ğ': 'G',
+      'İ': 'I',
+      'Ö': 'O',
+      'Ş': 'S',
+      'Ü': 'U',
+      'Â': 'A',
+      'Ê': 'E',
+      'Î': 'I',
+      'Ô': 'O',
+      'Û': 'U',
+    };
+    tr.forEach((k, v) => x = x.replaceAll(k, v));
+    return x.replaceAll(RegExp(r'\s+'), ' ').trim();
+  }
+
   String _birlesikRlKategoriAdi(String ad) {
-    var x = ad.trim();
-    x = x.replaceFirst(RegExp(r'\s+(Sağ|Sag|Sol)$', caseSensitive: false), '');
-    x = x.replaceFirst(RegExp(r'\s+R/L$', caseSensitive: false), '');
-    return x.trim();
+    var x = _asciiBuyuk(ad);
+    final rl = RegExp(r'(?:\s+R/L|\s+SAG|\s+SOL)$').hasMatch(x);
+    x = x.replaceFirst(RegExp(r'(?:\s+R/L|\s+SAG|\s+SOL)$'), '');
+    x = x.trim();
+    return rl && x.isNotEmpty ? '$x R/L' : x;
   }
 
   String _ustKategori(String ad) {
--- a/lib/services/arac_katalog_service.dart	2026-08-15 13:41:58.483501077 +0000
+++ b/lib/services/arac_katalog_service.dart	2026-08-15 13:41:19.250299809 +0000
@@ -476,6 +476,22 @@
     _aracAramaCacheTemizle();
   }
 
+
+  static String _parcaAdiStandartla(String raw, {String kategoriKodu = ''}) {
+    var x = _norm(raw).replaceAll('_', ' ').replaceAll(RegExp(r'\s+'), ' ').trim();
+    final kod = _birlesikKategoriKodu(kategoriKodu);
+    final rlKod = _rlKategoriAdlari.containsKey(kod);
+    final rlAd = RegExp(r'(?:\s+R/L|\s+SAG|\s+SOL)$').hasMatch(x);
+    x = x.replaceFirst(RegExp(r'(?:\s+R/L|\s+SAG|\s+SOL)$'), '').trim();
+    if ((rlKod || rlAd) && x.isNotEmpty) x = '$x R/L';
+    return x;
+  }
+
+  static String _ustKategoriStandartla(String raw) {
+    final x = _norm(raw.isEmpty ? 'DIGER' : raw).replaceAll('_', ' ');
+    return x.replaceAll(RegExp(r'\s+'), ' ').trim();
+  }
+
   static String _ozelKategoriKodu(String ustKategori, String parcaAdi) {
     final ust = _norm(ustKategori.isEmpty ? 'DIGER' : ustKategori);
     final ad = _norm(parcaAdi);
@@ -497,9 +513,9 @@
     required String parcaAdi,
     String oemKodu = '',
   }) async {
-    final ad = parcaAdi.trim();
+    final ad = _parcaAdiStandartla(parcaAdi);
     if (ad.isEmpty) throw Exception('Parça adı boş bırakılamaz.');
-    final ust = ustKategori.trim().isEmpty ? 'Diğer' : ustKategori.trim();
+    final ust = _ustKategoriStandartla(ustKategori);
     final kategoriKodu = _ozelKategoriKodu(ust, ad);
     final kodlar = _manuelOemleriAyir(oemKodu);
 
@@ -601,14 +617,6 @@
     required String oemKodu,
     String mevcutKategoriKodu = '',
   }) async {
-    final ad = parcaAdi.trim();
-    if (ad.isEmpty) throw Exception('Parça adı boş bırakılamaz.');
-    final ust = ustKategori.trim().isEmpty ? 'Diğer' : ustKategori.trim();
-    final temizOem = oemKodu.trim().toUpperCase();
-
-    // Kategori/parça türü araçtan bağımsızdır. Aynı kategori koduna bağlı
-    // bütün araçlarda ad ve üst kategori birlikte güncellenir. Kategori kodu
-    // sabit tutulur; böylece mevcut OEM ilişkileri ve benzersiz kayıtlar bozulmaz.
     var eskiKod = mevcutKategoriKodu.trim();
     if (eskiKod.isEmpty) {
       final mevcutRaw = await _db
@@ -622,6 +630,14 @@
       }
     }
 
+    final ad = _parcaAdiStandartla(parcaAdi, kategoriKodu: eskiKod);
+    if (ad.isEmpty) throw Exception('Parça adı boş bırakılamaz.');
+    final ust = _ustKategoriStandartla(ustKategori);
+    final temizOem = oemKodu.trim().toUpperCase();
+
+    // Kategori/parça türü araçtan bağımsızdır. Aynı kategori koduna bağlı
+    // bütün araçlarda ad ve üst kategori birlikte güncellenir. Kategori kodu
+    // sabit tutulur; böylece mevcut OEM ilişkileri ve benzersiz kayıtlar bozulmaz.
     if (eskiKod.isNotEmpty) {
       await _db
           .from('erp_arac_katalog_parcalar')
--- a/lib/app_config.dart	2026-08-15 13:41:58.484202034 +0000
+++ b/lib/app_config.dart	2026-08-15 13:41:19.250460239 +0000
@@ -2,7 +2,7 @@
   AppConfig._();
 
   static const String appName = 'ÜNAL YEDEK PARÇA ERP';
-  static const String version = '2.5.15';
+  static const String version = '2.5.16';
   static const String build = '20260815-mobile-operations';
 
   // Üretimde --dart-define ile değiştirilebilir. Varsayılan değerler mevcut
--- a/pubspec.yaml	2026-08-15 13:41:58.484258298 +0000
+++ b/pubspec.yaml	2026-08-15 13:41:19.250532133 +0000
@@ -2,7 +2,7 @@
 description: PRO ERP
 publish_to: "none"
 
-version: 2.5.15+2026081503
+version: 2.5.16+2026081504
 
 environment:
   sdk: ">=2.18.0 <4.0.0"
